# سياسة الأمان

## الإصدارات المدعومة

يدعم فرع `main` فقط للتصحيحات الأمنية ما لم يذكر المستودع خلاف ذلك. لا تضع إصلاحات أمنية مباشرة في فرع إنتاجي؛ استخدم طلب سحب قابلًا للمراجعة مع فحوص مكتملة.

## الإبلاغ المسؤول

لا تنشر تفاصيل الاستغلال أو أي قيمة حساسة في مسألة عامة. استخدم قناة إبلاغ خاصة مع مالك المستودع، وضمّن وصف الأثر، وإصدار المشروع، وخطوات إعادة إنتاج آمنة، من دون بيانات مستخدمين أو مفاتيح أو توكنات.

## التعامل مع الأسرار

أي مفتاح أو توكن أو كلمة مرور أو شهادة خاصة تظهر في Git تعد مكشوفة. لا تضع أسرارًا أو قيَمًا تشبه بيانات الاعتماد في المصدر أو الأمثلة أو ملفات البناء أو متغيرات الواجهة العامة مثل `VITE_*` و`NEXT_PUBLIC_*` و`EXPO_PUBLIC_*`. استخدم متغيرات بيئة خادمية أو مخزن أسرار مناسبًا، ودوّر السر لدى مزوده قبل تنظيف التاريخ عند اكتشاف تعرض فعلي.

## سياسة الاعتمادات وسير العمل

لا تُدمج ثغرات حرجة معروفة. لا تسمح بثغرة عالية إلا باستثناء محدد المعرّف، له مالك وتاريخ مراجعة وتخفيف موثق. يجب أن تستخدم إجراءات التكامل المستمر أقل صلاحية ممكنة ومراجع مثبتة بـSHA، وألا تمرر أسرارًا إلى شيفرة طلبات سحب غير موثوقة.

## سياسة النشر

لا يُنشر تغيير قبل نجاح فحص الأسرار، تدقيق الاعتمادات ذي العتبة المعتمدة، وفحوص المشروع القابلة للتطبيق. لا يثبت نجاح هذه الفحوص خلو المنتج من العيوب، لكنه يمنع إعادة إدخال فئات مخاطر محددة ويترك أثرًا قابلًا للمراجعة.
