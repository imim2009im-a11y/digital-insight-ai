# إعداد الربط الخارجي لموقع Digital Insight AI

الكود أصبح جاهزاً للربط الخارجي، لكن بعض الخدمات تحتاج مفاتيح أو رموز من حساباتك أنت. لا تستخدم رموزاً وهمية؛ لن تعمل.

## 1. نموذج التواصل

في ملف `contact.html` ابحث عن هذه القيمة:

```html
REPLACE_WITH_FORMSPREE_CONTACT_ENDPOINT
```

واستبدلها برابط النموذج الحقيقي من خدمة استقبال النماذج التي تختارها.

## 2. نموذج النشرة البريدية

في ملف `index.html` ابحث عن هذه القيمة:

```html
REPLACE_WITH_FORMSPREE_NEWSLETTER_ENDPOINT
```

واستبدلها برابط نموذج النشرة الحقيقي.

الأفضل أن تستخدم نموذجين منفصلين:

- نموذج لرسائل التواصل.
- نموذج لاشتراكات النشرة.

## 3. تتبع نقرات الأفلييت

تم تفعيل تتبع محلي لنقرات روابط الأفلييت في `script.js`.

ما يحدث حالياً:

- إضافة وسوم UTM إلى روابط GamsGo تلقائياً.
- تسجيل النقرات محلياً داخل المتصفح باستخدام `localStorage`.
- إرسال حدث `affiliate_click` إلى Google Analytics إذا أضفت Google Tag لاحقاً.

لعرض النقرات المحلية افتح:

```text
analytics.html
```

تنبيه صريح: هذه اللوحة محلية فقط. إذا فتحت الموقع من جهاز آخر لن ترى نفس البيانات.

## 4. Google Analytics

بعد إنشاء حساب القياس، أضف Google Tag داخل وسم `<head>` في الصفحات المهمة. بعد ذلك سيبدأ `script.js` بإرسال حدث:

```text
affiliate_click
```

مع بيانات الأداة والرابط والصفحة.

## 5. Google Search Console

بعد إضافة الموقع في Search Console، اختر طريقة تحقق واحدة:

- HTML tag: أضف وسم التحقق داخل `<head>` في `index.html`.
- HTML file: ارفع ملف التحقق الذي يعطيك Google إلى جذر المستودع.

لا يمكنني إنشاء رمز التحقق نيابةً عنك؛ الرمز يصدر من حسابك فقط.

## 6. سياسة الخصوصية

قبل الإطلاق التجاري، عدّل `privacy-policy.html` وأضف بوضوح أن الموقع قد يستخدم:

- التخزين المحلي لتسجيل نقرات الأفلييت لأغراض الاختبار.
- Google Analytics عند تفعيله.
- خدمة نماذج خارجية لمعالجة الرسائل والاشتراكات.

## 7. اختبار سريع

1. افتح `tools.html`.
2. اضغط رابط أفلييت واحد.
3. افتح `analytics.html` وتأكد أن النقرة ظهرت.
4. جرّب نموذج التواصل.
5. جرّب نموذج النشرة.
6. افتح `robots.txt` وتأكد أن رابط sitemap موجود.
7. افتح `sitemap.xml` وتأكد أن الصفحات الأساسية موجودة.
